enum DatabaseFields {
  courses,
  exams,
  completedSections,
  courseId,
  examId,
  currentSectionId,
  completionStatus,
  attempts,
  attemptId,
  startTime,
  endTime,
  score,
  responses,
}

class HiveConfig {
  static final String usersBoxName = 'users';
}
